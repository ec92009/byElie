# byElie Codex Review 2026-05-05

Generated: 2026-05-05 10:36:54 CEST

1/ General architecture

- byElie is a focused static portfolio with clear pages, shared CSS, assets, and a small maintenance script. The repo shape is good for GitHub Pages.
- The site repeats large inline SVG illustration blocks in HTML. That makes pages heavy and harder to maintain. Move repeated illustration/companion markup into reusable includes generated by the maintenance script, or use external assets.
- There is a pre-existing dirty git state showing a deleted older Claude review. I did not touch that unrelated state.

2/ UI

- The service carousel, proof metrics, and page navigation communicate the offer well. The visual identity is distinctive.
- The header navigation is text-heavy on small screens. Test mobile wrapping and consider a compact menu if links crowd the brand/version area.

3/ UX

- The contact CTA currently uses `mailto:` with no address. That is a conversion blocker. Add a real email address or a lightweight contact form/service.
- The offers are broad. Add one "best first step" path for new visitors who do not know whether they need CAD, prints, plans, or web work.

4/ Testing

- Add static smoke checks for missing local assets, empty links, canonical URLs, and version consistency across HTML cache-bust query strings.
- Add a mobile screenshot check for the header and carousel.

5/ Everything else

- The versioning discipline is good, but repo-only review docs should not require a visible version bump.
- Keep generated sitemap/robots updates part of the maintenance script.

6/ My suggetions:

1. Replace empty `mailto:` with a real contact path.
2. Add a static asset/link/version consistency checker.
3. Extract repeated SVG companion markup out of page HTML.
4. Add a compact mobile navigation treatment.
5. Add a "Start here" section that routes visitors by need.
